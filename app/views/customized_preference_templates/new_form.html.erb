<h1>
  Add a new customized preference
</h1>

<!--CREATING A SINGLE EVENT BEFORE CUSTOMIZED PREFERENCES FLOW WILL BE ABLE TO WORK-->

<hr>

<form action="/create_customized_preference" method="post">
  
  <div class="form-group">
    <label for="event_id">
      Event
    </label>

    <%= select_tag("event_id", options_from_collection_for_select(Event.all, :id, :name), :class => "form-control") %>
    or <a href="/events/new">Add a new event</a> first
  </div>



  <div class="form-group">
    <input type="hidden" id="user_id" name="user_id" value="<%=current_user.id%>" class="form-control">
  </div>


<div class="form-group">
    <label for="goal_id">
      Goal
    </label>

    <%= select_tag("goal_id", options_from_collection_for_select(Goal.where(user_id: current_user.id), :id, :name), :class => "form-control") %>
    or <a href="/goals/new">Add a new goal</a> first
  </div>

  <div class="form-group">
    <label for="transaction_amount">
      Transaction amount
    </label>
    <%= select_tag("transaction_amount", options_for_select([['$0.25', 0.25], ['$0.50', 0.5], ['$1', 1], ['$2', 2], ['$5', 5], ['$10', 10]]), :class => "form-control") %>
  </div>
  
  
  
  <button class="btn btn-block btn-outline-secondary">
    Create customized preference
  </button>
</form>

<hr>

<a href="/customized_preferences">
  Go back
</a>
